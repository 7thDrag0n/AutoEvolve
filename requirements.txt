# AutoEvolve requirements
# Install with: pip install -r requirements.txt
# pip install -U --upgrade-strategy only-if-needed -r requirements.txt

# Core
pip
pyyaml>=6.0
pandas>=2.0
numpy>=1.24
requests>=2.31.0          # required for FreqTrade REST API reload on Windows
streamlit>=1.35.0
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
websockets>=12.0

# LLM providers (install the one you need)
anthropic>=0.34.0          # for provider: anthropic
openai>=1.40.0           # for provider: openai
# (ollama uses requests, already listed above)

# FreqTrade (install separately per official docs)
# https://www.freqtrade.io/en/stable/installation/

